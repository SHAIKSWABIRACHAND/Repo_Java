package telusuko.Enum;

public enum Status{ //status is also a class while Running Failed PEnding and Success are objects
    Running,Failed,Pending,Success;
}

// enum isa special class in Java that represents a group of constants (unchangeable variables, like final variables).
// this doesn't support extention like other classses but apart from that everything is same

